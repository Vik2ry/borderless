# --- Core Django ---
DEBUG=True
SECRET_KEY=change-me
ALLOWED_HOSTS=localhost,127.0.0.1

# CSRF / CORS lockdown
CSRF_TRUSTED_ORIGINS=https://api.example.com,https://app.example.com
CORS_ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com
CORS_ALLOW_CREDENTIALS=True

# HTTPS hardening (enable in prod)
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=0

# Admin API token (if you expose admin-only endpoints)
ADMIN_API_TOKEN=borderinter

# Fixed FX overrides (comma-separated list of A->B:rate)
# Example: USDx->cNGN:1576.5,EURx->USDx:1.08
FIXED_RATE_OVERRIDES=

# Requests timeout (seconds) for live FX
FX_HTTP_TIMEOUT=2.5

# Email (for password resets / verification) - defaults to console backend
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=
EMAIL_PORT=
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=
